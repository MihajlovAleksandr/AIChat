<?xml version="1.0" encoding="utf-8"?>
<!-- ВСТАВИТЬ ВНУТРЬ fragment_chat.xml, в тот же ConstraintLayout, где находится rv_messages и input_panel -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/unread_jump_container"
    android:layout_width="wrap_content"
    android:layout_height="40dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="12dp"
    android:background="@drawable/bg_unread_jump"
    android:clickable="true"
    android:elevation="8dp"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:gravity="center"
    android:orientation="horizontal"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:visibility="gone"
    app:layout_constraintBottom_toTopOf="@id/input_panel"
    app:layout_constraintEnd_toEndOf="parent">

    <TextView
        android:id="@+id/unread_jump_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1"
        android:textColor="@android:color/white"
        android:textSize="13sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/unread_jump_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="6dp"
        android:src="@drawable/ic_keyboard_arrow_down"
        app:tint="@android:color/white" />

</LinearLayout>
