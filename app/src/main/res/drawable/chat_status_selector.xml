<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Неактивный чат (завершенный) -->
    <item android:state_enabled="false" android:drawable="@drawable/circle_gray" />

    <!-- Активный чат (по умолчанию) -->
    <item android:drawable="@drawable/circle_teal" />
</selector>